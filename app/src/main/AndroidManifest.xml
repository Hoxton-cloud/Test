<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wsrfood1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/img"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WSRFood1">
        <activity
            android:name=".presentation.ui.login.SignInActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_up" />
        <activity
            android:name=".presentation.ui.login.SignUpActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_in" />
        <activity
            android:name=".presentation.MainScreenActivity"
            android:exported="false"
            android:label="@string/title_activity_main_screen" />
        <activity
            android:name=".presentation.OnBoardingActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>